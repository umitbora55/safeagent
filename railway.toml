[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "safeagent run"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 5

[service]
  internalPort = 18789
