[package]
name = "safeagent-shared-identity"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
x509-parser = "0.15"

[dev-dependencies]
rcgen = "0.13"
